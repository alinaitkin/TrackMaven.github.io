web:
  image: nginx
  ports:
    - 8080:80
  volumes:
    - output:/usr/share/nginx/html:ro
builder:
  build: .
  command: "gulp --require coffee-script/register"
  ports:
    - "35729:35729"
  volumes:
    - .:/code
